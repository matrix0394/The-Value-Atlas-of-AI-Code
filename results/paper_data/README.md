This directory contains publication-ready paper tables released with the
manuscript.

Included here:

- `regression_data.csv`

This is the released regression table associated with the paper figures and
supplementary source data. It is distinct from
`data/external/regression_covariates.csv`, which is only the upstream covariate
lookup used when regenerating this paper table from the coordinate data.
