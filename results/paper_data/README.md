This directory is the target location for publication-ready paper tables
generated by `src/run/run_paper_analysis.py`.

The key released table is:

- `regression_data.csv`

In the public workflow, the same file is also included in the Dryad package so
that reviewers can inspect the released paper table directly without rerunning
the analysis.
